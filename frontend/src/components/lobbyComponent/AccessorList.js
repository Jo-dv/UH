const AccessorsList = () => {
  return (
    <div>
      <div>
        {/* {props.accessors.map((accessor, i) => (
          <p i={i}>{accessor.name}</p>
        ))} */}
      </div>
    </div>
  );
};

export default AccessorsList;
